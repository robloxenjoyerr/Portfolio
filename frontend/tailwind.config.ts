import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        bebas: ['BebasNeueRegular', 'sans-serif'],
      },
    },
  },
};

export default config;
